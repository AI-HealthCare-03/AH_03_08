services:
  # 1. Redis (메시지 브로커 및 캐싱)
  redis:
    image: redis:alpine
    container_name: redis
    networks:
      - ws
    ports:
      - "6379:6379"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  # 2. Database
  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      TZ: 'Asia/Seoul'
    ports:
      - ${DB_EXPOSE_PORT}:${DB_PORT}
    networks:
      - ws
    volumes:
      - mysql_data:/var/lib/mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --port=${DB_PORT}
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 3. API Server (FastAPI)
  fastapi:
    container_name: fastapi
    build:
      context: .
      dockerfile: app/Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    image: ${DOCKER_USER}/${DOCKER_REPOSITORY}:app-${APP_VERSION}
    env_file: .env
    environment:
      - PYTHONIOENCODING=utf-8
      - PYTHONUTF8=1
    command: |
      sh -c "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    volumes:
      - ./app:/app/app
      - static_volume:/app/templates
      - /tmp/uploads:/tmp/uploads
      - ./data:/app/data
    restart: always
    ports:
      - "8000:8000"
    networks:
      - ws
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # 4. AI Worker (OCR 처리)
  ai-worker:
    container_name: ai-worker
    build:
      context: .
      dockerfile: ai_worker/Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    image: ${DOCKER_USER}/${DOCKER_REPOSITORY}:ai-${AI_WORKER_VERSION}
    env_file: .env
    environment:
      - PYTHONIOENCODING=utf-8
      - PYTHONUTF8=1
    command: uv run --no-sync celery -A ai_worker.celery_app worker -Q image -c 2 --loglevel=info
    restart: always
    mem_limit: 4G
    networks:
      - ws
    volumes:
      - ${PILL_MODEL_BASE_PATH}:/app/models
      - /tmp/uploads:/tmp/uploads
      - ./data:/app/data
      - ./ai_worker:/app/ai_worker
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # 5. LLM Worker (복약 가이드 생성 / 챗봇)
  llm-worker:
    container_name: llm-worker
    build:
      context: .
      dockerfile: ai_worker/Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    image: ${DOCKER_USER}/${DOCKER_REPOSITORY}:ai-${AI_WORKER_VERSION}
    env_file: .env
    environment:
      - PYTHONIOENCODING=utf-8
      - PYTHONUTF8=1
    command: uv run --no-sync celery -A ai_worker.celery_app worker -Q llm,tts -c 2 --loglevel=info
    restart: always
    mem_limit: 4G
    volumes:
      - chroma_data:/data/chromadb
      - ./data:/app/data
      - ./ai_worker:/app/ai_worker
      - ./scripts:/app/scripts
      - ./app:/app/app
    networks:
      - ws
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # 6. Celery Beat (스케줄러 - 복약 알림, 데일리 TIP)
  celery-beat:
    container_name: celery-beat
    build:
      context: .
      dockerfile: ai_worker/Dockerfile
    image: ${DOCKER_USER}/${DOCKER_REPOSITORY}:ai-${AI_WORKER_VERSION}
    env_file: .env
    environment:
      - PYTHONIOENCODING=utf-8
      - PYTHONUTF8=1
    command: uv run --no-sync celery -A ai_worker.celery_app beat --loglevel=info
    restart: always
    networks:
      - ws
    volumes:
      - ./ai_worker:/app/ai_worker
    depends_on:
      - redis

  # 7. Nginx (웹 서버 - 요청 리버스 프록시로 전달)
  nginx:
    container_name: nginx
    image: nginx:latest
    volumes:
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/vol/web/static
    ports:
      - "80:80"
    networks:
      - ws
    depends_on:
      - fastapi

  frontend:
    container_name: frontend
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./frontend:/app
    command: sh -c "npm install && npm run dev -- --host"
    ports:
      - "5173:5173"
    networks:
      - ws
    depends_on:
      - fastapi

volumes:
  mysql_data:
  static_volume:
  chroma_data:

networks:
  ws:
    driver: bridge